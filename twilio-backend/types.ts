export type ProxyService = {
    serviceSid: string,
    phoneNumber: string,
    driverName: string
}

export type ProxySession = {
    phoneNumber: string,
    serviceSid: string,
    sessionSid: string
}
